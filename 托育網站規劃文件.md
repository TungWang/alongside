# 育兒同行 Alongside — 完整規劃文件

> **專案名稱**：育兒同行（英文：Alongside）
> **命名意象**：陪伴／並肩同行。傳達「陪家長一起走過育兒與找托育這段路」，是協助與同行，而非做決定或掛保證——刻意避開「安全把關」這類會造成過度承諾與責任風險的定位。
> **視覺方向**：溫馨手作感 + 奶茶／大地色系（詳見文末「設計規格」）。奶油白底、焦糖奶茶棕重點色、圓角柔和卡片、手繪插圖點綴、圓潤親切字體、手機優先；避開冷硬科技藍與盾牌、勾勾等保證式符號，也刻意避開常見的陶土紅預設配色。
>
> 目標對象：家有 0–3 歲小孩、正在尋找優質托育機構的父母（涵蓋衛福部管轄的托嬰中心與教育部管轄的幼兒園）。
>
> 核心原則：不外包、不做人工資料整理、不使用會違反網站條款或觸法的爬蟲手段、成本壓到最低、以「網站能養活自己」為目標。
>
> *命名待辦：正式使用前，確認「育兒同行」與「Alongside」在親子／托育領域無現有品牌撞名，且對應網域仍可註冊。「Alongside」為常見英文字，網域可能需搭配字尾（如 alongside.tw、alongsideparents 等）。*

---

## 一、專案定位與範圍收斂

經過資料源查核，台灣沒有任何「全國級」的開放資料，且教育部全國教保資訊網設有驗證碼與防爬蟲機制，全國性自動化不可行且有風險。因此本專案定位為：

- **形式**：網站（Web），非 App。低頻需求適合用搜尋進站、看完即走，不需要下載。
- **地理範圍**：**新北市單一起步**。經查核，新北市的幼兒園與托嬰中心名冊都有可下載的開放資料（CSV/JSON/API）；台北市則完全沒有機構名冊開放資料（僅有統計數字），要納入就得靠爬有驗證碼的網站或手動下載清洗，違反本專案原則，故台北市本版不做。
- **資料來源**：只使用政府主動開放的資料；地圖與家長評價改用 Google 地圖連結（家長點擊才開啟，不呼叫 API、零費用）。不碰有驗證碼的教保網、不寫繞過防爬蟲的程式。
- **變現目標**：以廣告補貼營運費，讓網站自給自足，賺錢為其次。

### 明確排除項

- 不做全國覆蓋（資料拿不到、風險高）
- 不破解驗證碼、不繞過防爬蟲機制
- 不自建違規紀錄資料庫（改放官方查詢連結）
- 不做保母（居家托育）完整名冊（隱私法規限制，無完整開放資料）
- 不做直升體系（無任何公開資料）
- 不向園所收費（B2B 非你意願）

---

## 二、資料來源與處理方式

下表為六大需求項目對應的實際做法。原則：能用官方開放資料就用，不能就導向官方連結，絕不冒險爬取。

| 需求項目 | 資料來源 | 處理方式 | 風險 |
| --- | --- | --- | --- |
| 機構基本資料（名稱、地址、電話、人數、公私立） | 新北市開放資料 API（免金鑰 JSON） | 定期下載，產生靜態頁 | 低 |
| 收退費基準 | 新北市開放資料未含此欄位 | 放官方頁連結 | 低 |
| 過去評價 | Google 地圖 | 每間機構產生 Google 地圖連結，家長點擊去看評價（不呼叫 API、零費用） | 零 |
| 地圖／位置 | Google 地圖 | 同上，用同一個連結開啟地圖（免自行 geocoding 補座標） | 零 |
| 違規／裁罰紀錄 | 不自行抓取 | 每間機構頁放「查詢官方裁罰紀錄」連結，導向教保網 / 社會局 | 零 |
| 官方評鑑結果 | 不自行抓取（同上） | 放官方查詢連結 | 零 |
| 政府補助 | 官方頁面 | 放教育部 / 衛福部 / 新北市補助頁連結，不做試算 | 零 |
| 教育理念 | 園所官網（有餘力再做） | LLM 自動抓官網文字並分類，抓不到就留空 | 中（品質不穩） |
| 直升體系 | 無 | 本版不做 | — |

### 為什麼違規紀錄改放連結

違規資料有三重問題：教保網有防爬蟲與驗證碼、裁罰資料過期會下架、22 縣市社會局格式各異。自行抓取不僅技術與法律風險高，還可能呈現不完整或過時的違規紀錄而誤導家長。放官方查詢連結，你零風險、零維護，家長也拿到最權威來源。你的價值在於「把散落資訊集中成一頁，並精準導向正確的官方查詢點」。

---

## 三、起步範圍：新北市（單一起步）

起步範圍定為**新北市**。新北市人口最多、托育機構數量大、家長需求高，對「靠自然搜尋流量養活自己」有利；且經查核，新北市的幼兒園與托嬰中心名冊皆有可下載的開放資料，完全落在本專案「只用開放資料、不爬蟲、不手動整理」的原則內。台北市因無任何機構名冊開放資料，本版不做（見第一節）。

### 已確認的資料源（新北市）

| 資料集 | 發布機關 | 格式 | 更新頻率 |
| --- | --- | --- | --- |
| 新北市公私立立案幼兒園資料 | 新北市政府教育局 | CSV / JSON / XML / OpenAPI | 不定期～每年 |
| 新北市私立托嬰機構名冊 | 新北市政府社會局 | CSV / JSON / XML / OpenAPI | 每年 |
| 新北市公共托育中心名冊 | 新北市政府社會局 | CSV / JSON / XML / OpenAPI | 每年 |

> 註：公立／非營利幼兒園在來源端另有獨立名錄，實作時一併納入。托嬰中心的公立與私立是兩個資料集，需合併。

### 已知的兩個實作限制（重要，已納入設計；欄位皆為實測 API 回傳確認）

1. **沒有立案字號／設立許可字號可當主鍵。** 三份資料都只有流水號 `no`，且會隨資料重新發布而變動、不具永久唯一性，各資料集也各自獨立。因此本專案在系統內自行產生穩定主鍵（`type + 正規化名稱 + 行政區`），並以「機構名稱 + 地址」做跨來源比對（見第四節）。所幸違規／評鑑本來就走「官方查詢連結」而非自動串接，此限制對主線影響小。

2. **三個資料集全都沒有經緯度，只有文字地址。**（實測推翻了先前文件「幼兒園有 `wgs84` 座標」的說法——實際回傳沒有座標，也沒有 `recruit`。）但這**不影響本專案**：地圖與位置改用 Google 地圖連結（家長點名稱+地址組成的連結即開啟 Google 地圖），因此不需要自行把地址轉成座標、不需要 geocoding。地址只用來組連結，Google 搜尋容錯佳，不必清洗。

### 實測確認的欄位對照（三個資料集格式不同，故腳本分開處理）

| 統一欄位 | 幼兒園 | 私立托嬰 | 公共托育 |
| --- | --- | --- | --- |
| 名稱 | `title` | `title` | `name` |
| 電話 | `tel` | `localcallservice` | `localcallservice` |
| 行政區 | `district`（純區名） | `area`（含「新北市」需去除） | `town` |
| 收托人數 | 無 | `person` | 無 |
| 屬性 | `type`（公立等） | 固定「私立」 | 固定「公共托育」 |
| 受託單位 | 無 | 無 | `unit` |
| 座標 | 無 | 無 | 無 |

**產出**：三份資料集的 API 端點與欄位皆已實測確認（見第八節腳本），可直接進入開發。

---

## 四、資料串接設計

以「機構」為中心，把不同來源掛在同一筆資料上。

- **主鍵**：新北市開放資料**不含立案字號**，只有各自獨立的內部流水號。因此在你的系統內自行產生一組穩定 ID（例如「類型縮寫 + 正規化名稱 + 行政區」的雜湊），作為每間機構的主鍵。
- **串接邏輯**：政府名冊提供基本資料 → 用機構名稱 + 地址組成一個 Google 地圖搜尋連結（`mapUrl`），地圖、位置、評價都靠這個連結，家長點擊才開啟 Google。不呼叫任何 API、不需座標、不需 place_id。
- **名稱正規化（比對關鍵）**：因為沒有官方字號，跨來源與跟 Google 比對都靠名稱+地址。務必先正規化：去空白、統一全半形、拿掉「私立／附設／新北市」等前後綴差異，降低比對誤差。比對不到的少量機構標記出來人工確認（少量例外，非整批整理）。

---

## 五、技術架構（以 Firebase Hosting 為主、靜態優先）

核心思路：**定期抓資料 → 產生靜態頁面 → 部署到 Firebase Hosting 的免費方案**。這是把成本壓到趨近於零的關鍵。

> **關鍵前提：維持「純靜態」才能保持 0 成本。**
> Firebase Hosting 本身只是託管你產生好的 HTML 檔案，在免費方案（Spark）內託管靜態站，實務上是 0 元。但 Firebase 不只是 Hosting——它還有 Firestore、Cloud Functions、Storage 等服務，一旦用到這些（尤其是排程用的 Cloud Functions），就會被推去綁信用卡、按用量計費的付費方案（Blaze）。因此本專案的成本紅線是：**所有資料在本機處理完、產生純 HTML 後才上傳，網站上線後不依賴任何 Firebase 後端服務。** 只要守住這條線，Hosting 這塊就維持 0。

### 建議架構

1. **資料抓取腳本**（在你自己電腦或 GitHub Actions 上跑，不放 Firebase）
   - 定期（如每月）下載新北市三個開放資料集（免金鑰 JSON API）
   - 為每間機構組一個 Google 地圖連結（地圖與評價都靠它，不呼叫任何 API）
   - 產出結構化資料（JSON）
   - 這一步刻意留在本機或免費的 GitHub Actions 上，避免動用 Firebase 需綁卡的後端服務。

2. **靜態網站產生器**（如 Astro、Next.js 靜態輸出、Hugo 等）
   - 讀取上一步的 JSON，每間機構產生一個獨立、純靜態的 HTML 頁面
   - 頁面標題含「地區 + 機構名 + 托嬰中心／幼兒園」，利於 Google 收錄
   - 產出的必須是純前端檔案（HTML/CSS/JS），不含需要伺服器運算的功能。

3. **Firebase Hosting 託管**
   - 用 Firebase CLI 把產生好的靜態檔案部署上去
   - 免費方案（Spark）額度：約每月 10 GB 儲存、360 MB/日 傳輸，對文字為主的單一縣市靜態站綽綽有餘
   - 自動附 HTTPS、全球 CDN、免費 `xxx.web.app` 子網域
   - 沒有伺服器要維護，安全性高

### 維持 0 成本的紀律（務必遵守）

- **不用 Cloud Functions**：排程/後端邏輯一律放本機或 GitHub Actions，不放 Firebase（用了就得升 Blaze）。
- **不用 Firestore / Realtime Database 當網站後端**：資料以靜態 JSON 或直接編進 HTML 呈現，不即時查資料庫。
- **不用 Firebase Storage 放大量圖片**：圖片盡量少、壓縮好，隨靜態檔一起部署；避免額外服務額度。
- **若哪天真的升級到 Blaze**（例如為了自動更新資料）：Firebase 會要求綁信用卡，即使只用免費額度內的量。此時務必到後台設定**用量預算警示 / 上限**，避免設定錯誤或異常呼叫產生預期外帳單。（綁卡與設定由你本人在 Firebase 後台操作。）

### 為什麼完全沒有 API 費用

- 地圖與家長評價都改用 **Google 地圖連結**：家長點擊才由他們自己的瀏覽器開啟 Google，你的網站與腳本**不呼叫任何 API**，因此沒有金鑰、沒有用量、沒有費用，也不需要快取或用量上限設定。
- 唯一的外部請求是抓新北市開放資料，而那是**免金鑰、免費**的政府 API，且只在每月更新時抓，與訪客流量無關。

### 成本估算（新北市、Firebase 靜態站）

| 項目 | 金額 | 說明 |
| --- | --- | --- |
| Firebase Hosting | **0**（免費方案內） | 維持純靜態、不動用需綁卡的後端服務即可 |
| 網域 | 約 300–600 元/年（可省成 0） | 用自訂網域才需付費；接受 `xxx.web.app` 免費子網域則 0 |
| 地圖／評價 | **0** | 走 Google 地圖連結，不呼叫 API |
| 開放資料 API | **0** | 新北市 API 免金鑰、免費 |
| 你的時間 | — | 不外包，主要投入 |

**結論**：只要守住「純靜態」紀律，現金支出可壓到 **每月 0 元**；唯一可能的固定支出是每年一個網域費（300–600 元），若接受免費子網域則連這個都省下。「養活自己」門檻極低，甚至不需要靠廣告就能維持存在。

---

## 六、變現：讓網站養活自己

定位是「補貼營運費、自給自足」，不是賺錢。

### 主要：Google AdSense

- 在機構列表頁、機構詳細頁放少量廣告版位。
- 收入靠自然搜尋流量。台灣中文內容 eCPM 偏低（約每千次頁面瀏覽 NT$10–50），但因你成本趨近於零，只要有一點流量即可打平。
- 別放過多廣告影響閱讀體驗，家長信任感是這類網站的命脈。

### 補充（流量起來後再加，皆不需你去談 B2B）

- **聯盟導購**：家長生命週期中會買嬰幼兒用品、保險、兒童課程等，放聯盟連結（affiliate）抽成，單價比純廣告高。務必與機構排序切開、清楚標示為廣告，維持公正性。
- **一次性小額解鎖**：未來若功能夠深（比較表、匯出等），可對進階功能收一次小額費用。低頻家長為「避免選錯」付一次小錢的意願，通常高於長期看廣告。

### 誠實的預期

- 初期流量小，廣告收入趨近於零，但支出也趨近於零，不至於虧。
- SEO 做起來後（單一縣市家長都會來查），廣告可望補貼掉營運費，達到大致打平。
- 想靠它賺錢很難，但「養活自己」在這個超低成本結構下是務實可達的目標。

---

## 七、如何發布網站（以 Firebase Hosting 為主）

以「純靜態網站 + Firebase Hosting 免費方案」為前提，最省成本的發布流程：

### 步驟 1：準備程式碼與內容
- 用靜態網站產生器（Astro / Next.js 靜態輸出 / Hugo 擇一）把資料 JSON 產生成純 HTML 頁面。
- 在本機先跑起來確認每間機構頁、搜尋、篩選都正常。
- 確認輸出的是純前端檔案（HTML/CSS/JS），沒有需要伺服器運算的部分——這是維持 0 成本的前提。

### 步驟 2：版本控管
- 把整個專案放上 GitHub（建一個 repository）。這同時是備份，也是後面自動部署的來源。

### 步驟 3：建立 Firebase 專案並部署
- 到 Firebase 主控台建立一個新專案（用 Google 帳號登入即可）。**維持免費的 Spark 方案，不要升級到 Blaze**，就不會被要求綁信用卡。
- 在本機安裝 Firebase CLI（透過 npm：`npm install -g firebase-tools`）。
- 執行 `firebase login` 登入你的 Google 帳號，再執行 `firebase init hosting`，選擇剛建立的專案，並把「public 資料夾」指向你靜態網站的輸出目錄。
- 執行 `firebase deploy` 就會把網站上線，並自動取得一個免費的 `xxx.web.app` 網址（附 HTTPS 與 CDN）。

> 注意：建立 Firebase 專案、用 Google 帳號登入、（若日後選擇）綁定付款方式等動作，請你自己在 Firebase 主控台與本機終端機完成（涉及帳號與授權，我不代為執行）。**只要不主動升級 Blaze、不啟用 Cloud Functions/Firestore/Storage，就能維持免費、不綁卡。**

### 步驟 4（可選）：綁定自訂網域
- 若想用自己的網域（而非 `xxx.web.app`）：到網域商（如 Cloudflare、Namecheap、Gandi 或台灣的 PChome 網域）購買一個網域（約 300–600 元/年）。
- 在 Firebase Hosting 的「新增自訂網域」流程中，依指示到網域商設定它給的 DNS 紀錄，Firebase 會自動配發 SSL 憑證。
- 若想維持完全 0 成本，可略過此步，直接使用免費子網域。
- 這些設定會需要你輸入帳號與付款資訊，請自行操作。

### 步驟 5：接上分析與廣告
- 申請 Google Search Console，提交網站 sitemap，讓 Google 收錄你的機構頁（SEO 命脈）。
- 申請 Google Analytics 看流量。
- 網站有一定內容與流量後，申請 Google AdSense（AdSense 對新站有審核，內容太少會被拒，所以先把機構頁做足再申請）。

### 步驟 6：資料更新機制
- 初期可每月在本機執行一次資料抓取腳本（下載開放資料 → 產生頁面）→ 執行 `firebase deploy` 重新部署。
- 要完全免費自動化，把整條流程放到 GitHub Actions（見下方第八節），排程每月自動跑，不用開自己的電腦、不用綁卡。

---

## 八、免費自動化更新資料（GitHub Actions）

關鍵原則：把整條流程放在 **GitHub Actions**（免費）上跑，而非 Firebase 需綁卡的 Cloud Functions。GitHub Actions 對公開 repo 完全免費，私有 repo 每月也有約 2,000 分鐘免費額度；你「每月跑一次、幾分鐘結束」的量遠遠用不完。

### 流程總覽

GitHub Actions 定時觸發 → 執行抓資料腳本（下載開放資料 → 為每間機構組 Google 地圖連結 → 產生靜態頁）→ 用 Firebase CLI 部署上線。全程在 GitHub 免費機器上完成。

### 為什麼這樣維持 0 成本

- **GitHub Actions**：免費額度內，用量微不足道。
- **Firebase Hosting**：純靜態部署，Spark 免費方案。
- **無任何 API 費用**：地圖與評價走 Google 地圖連結、開放資料 API 免金鑰免費，全程不呼叫付費 API。
- 全程沒用到 Firebase Cloud Functions（那個才需綁卡）。

### 設定步驟

1. 把抓資料+產頁腳本寫好（骨架見下），在本機先跑通。
2. 在 repo 建立 workflow 檔 `.github/workflows/update.yml`（內容見下），用 cron 設定每月觸發。
3. 把 Firebase 部署憑證放進 GitHub Secrets（**絕不寫進程式碼或 commit**）：
   - `FIREBASE_SERVICE_ACCOUNT`：在 Firebase 專案產生的 service account 金鑰（讓 Actions 能代你部署，不需互動式登入）。
   - （不需要 Google API 金鑰——地圖與評價都走連結，不呼叫 API。）
4. 正式排程前，先用手動觸發（`workflow_dispatch`）跑幾次確認流程無誤。

> 建立 Firebase 專案、產生 service account 金鑰、把金鑰貼進 GitHub Secrets 等動作，需你本人在各後台操作（涉及帳號與憑證，不代為執行）。金鑰一律放 Secrets，避免外洩被盜用。

### GitHub Actions workflow（`.github/workflows/update.yml`）

```yaml
name: 每月更新資料
on:
  schedule:
    - cron: '0 2 1 * *'   # 每月 1 號 02:00 UTC 觸發（資料源本就每月更新，不必更密）
  workflow_dispatch:        # 允許手動按鈕觸發，方便測試
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - name: 抓資料 + 產生靜態頁
        run: node scripts/fetch-and-build.js
      - name: 部署到 Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: 你的firebase專案ID
          channelId: live
```

### 抓資料 + 產頁腳本骨架（`scripts/fetch-and-build.js`）

> 這是 Node.js 骨架，已按新北市三個資料集的**實際 API 回傳欄位**填好（皆實測確認）。三個端點免申請金鑰、直接 GET。地圖與評價**改用 Google 地圖連結**（家長點擊才開啟 Google，不呼叫任何 API、零費用），因此不需要 geocoding、不需要 Google API 金鑰、也不需要評論快取。仍標 `TODO` 的只剩產頁 HTML 模板（你的設計）。

```javascript
// scripts/fetch-and-build.js
// 流程：下載三個開放資料集 → 正規化 → 產生 Google 地圖連結 → 產生靜態頁
// 地圖與評價都走連結：不呼叫任何 API、無金鑰、無快取、零費用
import fs from 'node:fs/promises';
import path from 'node:path';

const OUTPUT_DIR = 'public';                 // Firebase Hosting 的 public 目錄

// 新北市三個開放資料集（免金鑰，直接 GET /json；page 從 0 開始）
const BASE = 'https://data.ntpc.gov.tw/api/datasets';
const SOURCES = [
  { type: 'preschool',   subtype: '幼兒園',   oid: 'f563b4cd-b850-41f5-9709-b910f2d147e9' },
  { type: 'nursery',     subtype: '私立托嬰', oid: '69cecdb0-7796-48df-84e5-99e4f1274245' },
  { type: 'nursery_pub', subtype: '公共托育', oid: 'b3faf2aa-e96b-4f2f-b647-da47dc094860' },
];

// --- 名稱正規化：無官方字號，靠名稱+地址比對，先清乾淨 ---
function normalizeName(name) {
  return (name || '')
    .replace(/\s+/g, '')
    .replace(/[（）()]/g, '')
    .replace(/^(新北市|私立|市立|附設)/g, '')
    .trim();
}

// 取出純區名（三個資料集的行政區欄位格式不同）
function pickDistrict(r, type) {
  if (type === 'preschool')   return r.district;                          // 「新店區」
  if (type === 'nursery')     return (r.area || '').replace('新北市', ''); // 「新北市板橋區」→「板橋區」
  return r.town;                                                          // 公共托育：「汐止區」
}

// 自行產生穩定主鍵（來源無立案字號，且流水號 no 會變動不可信）
function makeId(type, name, district) {
  return `${type}-${normalizeName(name)}-${district || ''}`;
}

// 產生 Google 地圖搜尋連結（家長點擊才開啟 Google，含地圖、導航、評價）
function mapsUrl(name, district, address) {
  const query = `新北市${district || ''}${name} ${address || ''}`.trim();
  return `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(query)}`;
}

// --- 1. 分頁全量下載三個資料集，正規化成統一格式 ---
async function fetchAll(oid) {
  const out = [];
  const size = 500;
  for (let page = 0; ; page++) {   // page 從 0 開始
    const res = await fetch(`${BASE}/${oid}/json?page=${page}&size=${size}`);
    const rows = await res.json();
    if (!Array.isArray(rows) || rows.length === 0) break;
    out.push(...rows);
    if (rows.length < size) break;  // 最後一頁
  }
  return out;
}

async function fetchInstitutions() {
  const all = [];
  for (const src of SOURCES) {
    const rows = await fetchAll(src.oid);
    for (const r of rows) all.push(normalizeRow(r, src));
  }
  return all;
}

// 把三個資料集各自不同的欄位轉成統一結構（欄位名皆為實測確認）
function normalizeRow(r, src) {
  const t = src.type;
  const name = t === 'nursery_pub' ? r.name : r.title; // 公共托育用 name，其餘用 title
  const district = pickDistrict(r, t);
  const tel = t === 'preschool' ? r.tel : r.localcallservice;
  const capacity = t === 'nursery' ? (r.person || null) : null; // 只有私立托嬰有人數
  const publicPrivate =
    t === 'preschool'   ? r.type :            // 「公立」等
    t === 'nursery_pub' ? '公共托育' : '私立';

  return {
    type: t === 'preschool' ? 'preschool' : 'nursery',
    subtype: src.subtype,
    name,
    tel,
    district,
    address: r.address,
    capacity,
    publicPrivate,
    unit: r.unit || null,                       // 公共托育才有的受託單位
    mapUrl: mapsUrl(name, district, r.address),  // 地圖＋評價都靠這個連結
    id: makeId(t === 'preschool' ? 'preschool' : 'nursery', name, district),
  };
}

// --- 2. 產生靜態頁（每間機構一頁 + 首頁/列表 + sitemap）---
async function buildPages(list) {
  await fs.mkdir(OUTPUT_DIR, { recursive: true });

  // 每間機構一頁，檔名用 id，標題含「地區+機構名+類型」利於 SEO
  for (const inst of list) {
    const html = renderInstitutionPage(inst); // TODO: 套你的模板（記得放 inst.mapUrl 連結）
    await fs.writeFile(path.join(OUTPUT_DIR, `${inst.id}.html`), html);
  }
  await fs.writeFile(path.join(OUTPUT_DIR, 'index.html'), renderIndex(list));
  await fs.writeFile(path.join(OUTPUT_DIR, 'sitemap.xml'), renderSitemap(list));
}

function renderInstitutionPage(_inst) { return '<!doctype html>...'; } // TODO
function renderIndex(_list) { return '<!doctype html>...'; }          // TODO
function renderSitemap(_list) { return '<?xml version="1.0"?>...'; }   // TODO

// --- 主流程 ---
async function main() {
  const institutions = await fetchInstitutions();
  await buildPages(institutions);
  console.log(`完成：${institutions.length} 間機構`);
}

main().catch((e) => { console.error(e); process.exit(1); });
```

### 實務提醒

- **地圖與評價都靠連結，零 API 費用**：每間機構產生一個 Google 地圖搜尋連結（`inst.mapUrl`），家長點擊才開啟 Google，你不呼叫任何 API、不需金鑰、不需快取，成本為 0。頁面模板記得把這個連結放上去（例如「在 Google 地圖查看／看評價」按鈕）。
- **網站不顯示星等數字與評論內容**：那些留在 Google 地圖那邊，家長點連結去看。你的網站價值在「集中呈現、方便比較、一鍵跳轉」。
- **cron 不必太密**：資料源本就每年更新，每月跑一次已很充裕。
- **每次都是全量重建**：因為沒有 API 用量要省，腳本每次直接抓最新資料、全部重產頁面即可，不需要快取或增量邏輯，簡單可靠。
- **先手動測再自動**：正式排程前用 `workflow_dispatch` 手動跑幾次確認無誤。

---

## 九、建議執行順序（里程碑）

1. **資料源已確認**：新北市三個資料集的 API 端點與欄位皆已實測（見第八節腳本，端點已填好）。這步已完成，可直接進開發。
2. **最小可用版**：機構基本資料 + 每間一個 Google 地圖連結（地圖＋評價），新北市，靜態站，每間機構一頁。先上線。
3. **接上官方連結**：機構頁加違規查詢、評鑑、補助的官方連結。
4. **做 SEO**：提交 Search Console，確認機構頁被 Google 收錄，觀察自然流量。
5. **上廣告**：內容夠了申請 AdSense，目標補貼營運費。
6. **驗證與擴充**：確認家長真的會用、會搜到、會回訪後，再評估加教育理念分類、聯盟導購。台北市因無開放資料，若要擴充需另評估（例如接受定期手動下載官方公告清單當特例）。

---

## 十、風險與注意事項

- **資料時效**：開放資料更新頻率不一，違規/評鑑走官方連結。網站上所有整合資料應標註「資料擷取時間」與「以官方公告為準」，降低誤導風險。
- **Google 地圖連結**：只是產生一般外部連結導向 Google 地圖，不呼叫 API、不儲存 Google 資料，單純又無費用；星等與評論由家長在 Google 那邊查看。
- **不碰驗證碼與防爬蟲**：教保網等有驗證碼的站一律不自動抓取，改用開放資料或官方連結，這是本專案能長期安全存活的底線。
- **先有價值再想收費**：家長端要先做出「比零散 Google 搜尋更有用」的體驗，累積使用與口碑，變現是後話。
- **帳號與付款操作**：所有註冊、綁定、付款、授權連結等動作由你本人在各平台完成。

---

## 附錄：設計規格（交給 Code 前定案）

風格方向：**溫馨手作感 + 奶茶／大地色系**。氣質像一本溫暖的親子手札——柔和、有手感，讓焦慮中找托育的家長一進來就放鬆，但資訊清楚、好比較。手機優先（家長多用手機查）。

> 給 Code 的重要提醒：不要落入常見的 AI 預設「奶油底＋陶土紅（terracotta，約 `#D97757`）」配色——那已是通用預設，缺乏辨識度。本專案的大地色要往**奶茶棕、咖啡奶、暖灰米**這種「柔和沉穩、偏灰調」的方向走，刻意避開橘紅陶土感。重點色用溫潤的**焦糖奶茶棕**，而非鮮明陶土橘。

### 色彩 tokens（hex，供直接使用）

| 用途 | 名稱 | Hex | 說明 |
| --- | --- | --- | --- |
| 背景 | 奶油米白 | `#F7F3EC` | 不用純白，暖而柔 |
| 卡片底 | 暖白 | `#FFFDF9` | 比背景略亮，卡片浮起感 |
| 區塊分隔底 | 淺陶米 | `#EDE6DA` | 交錯區塊、頁尾 |
| 主色／重點 | 焦糖奶茶棕 | `#B08863` | 按鈕、連結、標題重點（偏灰的暖棕，非橘紅） |
| 主色深（hover） | 深奶茶 | `#8F6D4E` | 按鈕 hover、強調 |
| 文字主色 | 深棕灰 | `#3E362E` | 不用純黑，暖調 |
| 文字次要 | 灰棕 | `#7A7169` | 次要說明、備註 |
| 標籤：公立 | 暖綠 | `#7A8B6F` | 類型標籤，低飽和 |
| 標籤：私立 | 奶茶棕 | `#B08863` | 同主色系 |
| 標籤：公共托育 | 暖橘褐 | `#C08A5E` | 三類一眼可分，皆低飽和 |

原則：所有顏色低飽和、暖調、彼此接近，怎麼搭都和諧。標籤色只用來區分三種機構類型。

### 字體

- 中文：圓潤、親切但清楚的圓體（如思源黑體 Noto Sans TC，字重用 Regular/Medium；標題可用 Bold）。避免過細或過於正式的字體。
- 標題可略大而有個性，內文務必好讀——家長是來找資訊的，清楚永遠優先。
- 建議型階：標題 28–32px、區塊標題 20–22px、內文 16px、備註 14px（手機可略縮）。

### 形狀與質感

- 圓角：卡片與按鈕用較大圓角（卡片 `16px`、按鈕 `12px`），是手作感關鍵。
- 陰影：柔和淺陰影（如 `0 2px 12px rgba(62,54,46,0.06)`），讓卡片像輕浮在紙上；不要硬邊框、不要重陰影。
- 間距：區塊之間留白充足，不要擠（區塊間距建議 48–64px，卡片內距 20–24px）。

### 插圖（手作感靈魂，但克制）

- 首頁放一張溫暖手繪插圖（牽手、親子、小房子之類意象）當情緒招牌。
- 搜尋無結果的空狀態，放一張小插圖配一句溫暖的話，化解挫折。
- 其餘用簡單線條小圖示即可，別每個角落都塞插圖（會雜亂又拖慢載入）。
- **版權**：插圖只用可商用的開源插圖庫（如 unDraw、Open Doodles）或自繪，切勿隨意抓網路圖片。

### 版面結構

- 首頁：一句溫暖標語 + 主插圖 + 搜尋／篩選入口（選行政區、選類型）。
- 列表：卡片式，每張卡一間機構，卡上放名稱、行政區、類型標籤、一個「在 Google 地圖查看／看評價」按鈕（`inst.mapUrl`）。
- 機構頁：基本資料（名稱、地址、電話、收托人數、公私立、公共托育的受託單位）+ Google 地圖按鈕 + 違規／評鑑／補助的官方查詢連結。
- 手機優先：單欄、卡片堆疊、按鈕夠大好點。

### 文案語氣

陪伴、不催促、化解焦慮。用「一起看看附近有哪些」「慢慢比較，不用急」這類口吻，而非「保證優質」「安全首選」（避免過度承諾與責任風險）。空狀態與錯誤訊息也用溫暖但清楚的引導語。

### 品質底線（Code 務必做到）

- 響應式（手機優先，往上相容平板／桌機）。
- 鍵盤焦點可見、尊重 `prefers-reduced-motion`。
- 動效克制：頁面載入或卡片 hover 的微互動即可，不要滿頁動畫（過多動效反而顯得像隨手套版）。

**一句話總結**：奶油白底、焦糖奶茶棕重點色、圓角柔和卡片、幾張手繪插圖點綴、圓潤親切字體、充足留白、手機優先——溫暖得像親子手札，但資訊清楚好比較。

---

*本文件為可調整的規劃草案，建議邊做邊依實際查到的資料情況修正範圍。*
