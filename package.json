{
  "name": "alongside",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-data.js",
    "dev": "astro dev",
    "build": "npm run fetch && astro build && npm run verify && npm run cost",
    "build:offline": "astro build && npm run verify && npm run cost",
    "verify": "node scripts/verify.js",
    "preview": "astro preview",
    "cost": "node scripts/cost.js"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.0"
  }
}
