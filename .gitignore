node_modules/
dist/
.astro/
.DS_Store
.env
.env.*
.firebase/

# Firebase CLI 安裝的 AI 助理技能文件（Firestore／Auth／Data Connect 等）。
# 本專案是純靜態站，不使用任何 Firebase 後端服務，這些用不到；
# 需要時可用 firebase init 重新安裝。
.agents/
.claude/skills/
skills-lock.json
